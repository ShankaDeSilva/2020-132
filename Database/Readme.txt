1. Create new empty databse in postgresSQL(PGAdmin 12).
2. On the created databse right click and select Restore.
3. The popup window in file name choose the backup database file and the Role name select as "postgres". 
4. then click Restore.